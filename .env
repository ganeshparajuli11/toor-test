# API Configuration
# For production, set this to your server's API URL (e.g., https://yourserver.com/api)
# For local development, leave empty to use default localhost:3001/api
VITE_API_URL=

# API Timeout
VITE_API_TIMEOUT=30000

# Default Settings
VITE_DEFAULT_CURRENCY=USD
VITE_DEFAULT_LANGUAGE=en_US

# Note: RateHawk API credentials are managed through the Admin Settings panel
# Access Admin > Settings > API Configuration to set up RateHawk credentials
# Default sandbox credentials are included for testing