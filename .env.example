# Zanafly Travel Platform - Environment Configuration
# Copy this file to .env and update the values as needed

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL
# For production: Set to your server URL (e.g., https://api.yoursite.com/api)
# For local dev: Leave empty to use default localhost:3001/api
VITE_API_URL=

# API Request Timeout (in milliseconds)
VITE_API_TIMEOUT=30000

# =============================================================================
# Default Application Settings
# =============================================================================

# Default currency for prices
VITE_DEFAULT_CURRENCY=USD

# Default language
VITE_DEFAULT_LANGUAGE=en_US

# =============================================================================
# RateHawk API Configuration (Hotel Search)
# =============================================================================
# Note: RateHawk credentials are managed through the Admin Settings panel
# 1. Log in as admin
# 2. Go to Settings > API Configuration
# 3. Enter your RateHawk API Key and Key ID
#
# For testing, sandbox credentials are pre-configured in the system.
# To get production credentials, register at: https://www.ratehawk.com/

# =============================================================================
# Stripe Payment Configuration
# =============================================================================
# Note: Stripe credentials are managed through the Admin Settings panel
# 1. Log in as admin
# 2. Go to Settings > Payment Configuration
# 3. Enter your Stripe Publishable Key and Secret Key
#
# Get your Stripe keys from: https://dashboard.stripe.com/apikeys

# =============================================================================
# Server Configuration (for backend)
# =============================================================================

# Server port (default: 3001)
PORT=3001

# Node environment
NODE_ENV=development
